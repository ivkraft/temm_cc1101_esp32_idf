###############################################################################
# VS Code
###############################################################################
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json
*.code-workspace

###############################################################################
# ESP-IDF / CMake build artifacts
###############################################################################
# Стандартная папка сборки IDF
build/
# Если используете несколько build-директорий
build_*/
out/
dist/

# Ninja/CMake мусор (если генерится в корень, редко, но бывает)
CMakeFiles/
CMakeCache.txt
cmake_install.cmake
Makefile
compile_commands.json
CTestTestfile.cmake
*.ninja
.ninja_*
*.log

###############################################################################
# ESP-IDF project config outputs
###############################################################################
# Важно: sdkconfig часто машинозависимый (можно не коммитить)
# sdkconfig
# sdkconfig.old
# sdkconfig.ci
# sdkconfig.*.old

# Файлы менюконфига/дефконфига (обычно КОММИТЯТ sdkconfig.defaults)
# sdkconfig.defaults   <-- обычно оставляют в репо, НЕ игнорим

###############################################################################
# ESP-IDF managed components (IDF 5+)
###############################################################################
managed_components/
dependencies.lock
# Если вы используете managed components и хотите фиксировать версии,
# dependencies.lock можно КОММИТИТЬ — тогда удалите строку выше.

###############################################################################
# Firmware outputs (если вдруг появляются вне build/)
###############################################################################
*.elf
*.map
*.bin
*.hex
*.uf2

###############################################################################
# Core dumps / crash logs
###############################################################################
core
core.*
*.core
coredump*
panic*

###############################################################################
# Python virtualenv / tooling (если держите venv в проекте)
###############################################################################
.venv/
venv/
ENV/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/

###############################################################################
# OS cruft
###############################################################################
.DS_Store
Thumbs.db
